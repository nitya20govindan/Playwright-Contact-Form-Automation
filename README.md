# Playwright Contact Form Automation

## Overview
This project contains automated tests for the "Contact Us" form using Playwright. The tests cover both functional (successful form submission) and negative test cases (invalid inputs).

## Setup

### 1. Clone the repository
```bash
git clone <your-repository-url>
cd Playwright-Contact-Form-Automation
